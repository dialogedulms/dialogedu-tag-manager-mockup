import './globals.css'

export const metadata = {
  title: 'Tag Manager - dialogEDU',
  description: 'Tag Manager Mockup for dialogEDU LMS',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
